Task C — external read-only probe evidence (garden login expired; cluster inspection blocked)
Date: 2026-08-11
Suffix: ai-trust-1.ai-trust.shoot.gardener.cc-one.showroom.apeirora.eu

== garden login state ==
adminkubeconfig mint -> interactive OIDC ("Please visit http://localhost:8000/") -> timed out.
CONCLUSION: garden login expired; run prerequisites/login.sh before any live shoot inspection.
No apply/patch/delete performed. All findings below are from PUBLIC probes + repo source.

== FQDN label count (LE 10-label / 64-char limits) ==
25veqwflh7syq7fm-d.ai-trust-1.ai-trust.shoot.gardener.cc-one.showroom.apeirora.eu
= 9 labels, 79 chars.
Per-instance hosts add ONE label under the suffix -> 9 labels total (suffix itself is 8 labels).
LE wildcard *.<suffix> would be 9 labels too. Under LE's public-CA label ceiling.

== DNS (public, no cluster needed) ==
suffix apex            -> 130.214.18.166
working host           -> 130.214.18.166
RANDOM nonexistent host-> 130.214.18.166   <-- WILDCARD A-RECORD ALREADY RESOLVES
CONCLUSION: *.<suffix> already resolves to the mesh LB IP via shoot-dns-service.
Per-host DNSEntries / dns.gardener.cloud/dnsnames are REDUNDANT for reachability.

== Served TLS cert (openssl s_client :443) ==
issuer  = CN=Standard Platform Mesh Local CA
subject = CN=ai-trust-1-mesh
notBefore=Aug 10 2026  notAfter=Nov 12 2028
SAN: ai-trust-1.<suffix>, *.ai-trust-1.<suffix>, kcp.api.ai-trust-1.<suffix>
curl verify: FAIL (60) self-signed certificate in certificate chain
CONCLUSION: single static self-signed wildcard cert 'domain-certificate' covers ALL instances.
This is why ContentConfiguration must use in-cluster HTTP (strict TLS verify rejects self-signed).

== Operator routing model (operator/main.go) ==
- wireIngress(): reuses ONE shared listener env WILDCARD_LISTENER (default terminate-wildstar);
  creates only per-instance HTTPRoutes (<ns>-app, <ns>-keycloak) + a ReferenceGrant. NO per-instance listener.
- ensureWildcardListener(): if a listener named 'terminate-wildstar' already exists (stock mesh ships it),
  DOES NOTHING. Otherwise appends one *.<suffix> HTTPS listener reusing secret 'domain-certificate'.
- deleteRoutes(): on delete, removes only the two per-instance HTTPRoutes; never touches the listener/cert.
