apiVersion: v2
name: aitrust-msp-app
description: >-
  AI Trust Platform MSP provider — workload side. Deploys the operator (watches
  AITrustPlatformInstance and stamps a full app copy per instance), the api-syncagent
  (mirrors the CR between the consumer's kcp workspace and this shoot), and the portal
  content nginx (serves /pm-content.json for the marketplace tile).
type: application
version: 0.1.0
appVersion: "1.0.0"
